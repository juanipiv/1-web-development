def words_counter(my_text):
    """build a dictionary that count the words in a given text string

    my_text(str): a given txt string

    Output(dict): key = words, value = number of times
    """
    result = dict()
    
    return result
